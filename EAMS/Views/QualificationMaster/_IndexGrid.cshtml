@model IEnumerable<EAMS.View_Models.QualificationMasterIndxVM>
@using NonFactors.Mvc.Grid

    @(Html
    .Grid(Model)
    .Build(columns =>
    {
        columns.Add(model => model.QualificationTypeString).Titled("Qualification Type").Sortable(true);
        columns.Add(model => model.QualificationLevelString).Titled("Qualification Level");
        columns.Add(model => model.Qualification).Titled("Qualification");
        columns.Add()
        .Titled("Edit")
        .RenderedAs(o => Html.ActionLink("Edit", "Edit", new { id = o.QualificationId }));
        columns.Add()
        .Titled("Delete")
        .RenderedAs(o => Html.ActionLink("Delete", "Delete", new { id = o.QualificationId }));
    })
    .UsingFilterMode(GridFilterMode.Excel)
    .Empty("No data found")
    .Id("my-ajax-grid")
    .Pageable(pager =>
    {
        pager.PagesToDisplay = 2;
        pager.RowsPerPage = 10;
    })
    .Filterable()
    .Sortable()
    )
